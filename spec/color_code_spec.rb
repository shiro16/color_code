require 'spec_helper'

describe ColorCode do
  it 'has a version number' do
    expect(ColorCode::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
